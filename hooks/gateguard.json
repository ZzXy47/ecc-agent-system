# GateGuard 门控

硬性前置条件 — 任何 Edit、Write 或 Bash 调用前必须执行。

## 触发条件

- 任何 `Edit` 工具调用
- 任何 `Write` / `create_file` 工具调用
- 任何 `Bash` / `run_in_terminal` 工具调用

## 执行流程

### Step 1: 拦截
在工具调用前，拒绝首次操作请求，输出：
> GateGuard: 拒绝操作，要求收集事实

### Step 2: 收集事实
要求调用者提供以下三项事实（缺一不可）：

1. **依赖分析** — 列出所有导入/引用目标文件的文件
   - 必须用 grep/search 实际搜索，不凭记忆
   
2. **影响范围** — 列出受影响的公共函数/类/接口
   - 必须从文件中实际读取
   
3. **数据结构** — 如果涉及数据文件/配置文件，展示字段名和结构
   - 必须实际读取

### Step 3: 验证放行
确认三项事实全部提供后，输出：
> GateGuard: 放行

允许操作继续。

## 验证要求

不允许在事实收集完成前执行任何 Edit/Write/Bash。如果调用者跳过 GateGuard，conductor 必须中断执行并报告安全违规。

## 配置

```yaml
enabled: true
blocking: true
bypass: false
